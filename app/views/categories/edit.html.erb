<h1 class="text-center mt-4">カテゴリー名を編集する</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <%= render 'form' %>
    </div>

    <div div class="row text-center">
      <%= link_to '[ Cancel and return to categories listing ]',category_path(@category), class: "text-info text-decoration-none" %>
    </div>
  </div>
</div>
