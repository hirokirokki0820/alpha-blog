<h1 class="text-center mt-4">カテゴリー：<%= @category.name %></h1>

<div class="text-center mt-4">
  <% if logged_in? && current_user.admin? %>
    <%= link_to "カテゴリー名を編集する", edit_category_path(@category), class: "btn btn-outline-info" %>
  <% end %>
</div>

<h3 class="text-center mt-4">記事一覧</h3>

<div class="mt-3">
<%= paginate @articles %>
</div>

<%= render 'articles/article' %>

<div class="mt-3">
<%= paginate @articles %>
</div>
